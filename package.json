{
  "private": true,
  "scripts": {
    "release": "pnpm publish -r",
    "clean": "pnpx rimraf node_modules",
    "clean:all": "pnpx rimraf node_modules && pnpm run -r --parallel clean"
  },
  "volta": {
    "node": "20.10.0"
  },
  "dependencies": {
    "@wqdy/eslint-config": "workspace:^0.0.1"
  }
}
